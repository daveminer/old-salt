import type { ComponentSingleStyleConfig } from '@chakra-ui/theme'

import "@fontsource/trade-winds"

const Button: ComponentSingleStyleConfig = {
  baseStyle: {
    bg: '#d0c882',
    borderColor: 'black',
    borderRadius: 0,
    borderWidth: '.2rem',
    colorScheme: 'none',
    color: 'black',
  },
  variants: {
    base: {}
  },
  defaultProps: {
    variant: 'base'
  }
}

export default Button
//yeseva one yatra one underdog
